// Tailwind v4 is applied via the @tailwindcss/vite plugin (see vite.config.js),
// not via a PostCSS plugin. This file exists only to shadow an unrelated
// global ~/postcss.config.js (a Tailwind v3 setup for a different project)
// that cosmiconfig would otherwise find by walking up from this directory.
export default {};
